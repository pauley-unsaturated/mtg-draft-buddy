# TLA SEALED fine-tune of the production deck trunk (fast path, 2026-07-31).
# EXP-127 recipe verbatim, pointed at decks.sealed.parquet via source: sealed.
# Context (S2a controls, TLA sealed val): ceiling 0.9060 F1 / 0.9224 trophy;
# EXP-126 zero-shot 0.6928/0.7201 — the draft→sealed shift is ~21pt.
exp: EXP-130
task: deck
model: builder
set: TLA
source: sealed
stats: full
seed: 17
init_from: checkpoints/EXP-126
scaler_from: checkpoints/EXP-126
batch_builds: 256
epochs: 30
patience: 4
emb_dim: 128
heads: 8
blocks: 3
dropout: 0.1
weighting: uniform
land_lambda: 0.2
basics_lambda: 0.5
peak_lr: 1.5e-4
lr_warmup: 300
weight_decay: 0.01
grad_clip: 1.0
